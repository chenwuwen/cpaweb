import { ChangePostBodyPipe } from './change-post-body.pipe';

describe('DigitalTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangePostBodyPipe();
    expect(pipe).toBeTruthy();
  });
});
