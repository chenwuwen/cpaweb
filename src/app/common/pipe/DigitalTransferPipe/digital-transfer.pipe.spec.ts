import { DigitalTransferPipe } from './digital-transfer.pipe';

describe('DigitalTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitalTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
